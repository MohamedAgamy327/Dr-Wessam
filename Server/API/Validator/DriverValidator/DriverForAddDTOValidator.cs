using API.DTO.Driver;
using FluentValidation;
using Repository.IRepository;

namespace API.Validator.DriverValidator
{
    //public class DriverForAddDTOValidator : AbstractValidator<DriverForAddDTO>
    //{
    //    private readonly IDriverRepository driverRepository;
    //    public DriverForAddDTOValidator(IDriverRepository driverRepository)
    //    {
    //        this.driverRepository = driverRepository;

    //        RuleFor(x => x.NationalID).NotEmpty().NotNull();
    //        RuleFor(x => x.DriverName).NotEmpty().NotNull();
    //    }
    //}
}
